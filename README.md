# wled_preset_light
Wraps a WLED light entity to replace the palette selector with your saved WLED presets, exposed as the effect list in the native light panel. Color picking and brightness pass through transparently to the underlying light.
